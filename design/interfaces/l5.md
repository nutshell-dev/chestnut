# Interfaces L5 — 服务层接口

4 模块：Runtime、Cron、StatusService、Gateway。

模板加字段说明见主索引 [interfaces.md](../interfaces.md)。

---

## Runtime [capability, DI]

**生产方**：`l5_runtime`

**消费方**：

- `l6_daemon`（DI，daemon 主入口调 initialize / processBatch / processWithMessage / chat / abort / stop）
- `l6_assembly`（DI，装配期 createRuntime 工厂 + 注入 RuntimeDependencies）

**接口签名**（实然 = `class Runtime` + concrete L1-L5 deps DI / 不立 `Runtime` 抽象 interface / phase 429 13 port cluster 全清 ✓）：

```ts
export class Runtime {
  constructor(options: RuntimeOptions);

  // 生命周期
  initialize(): Promise<void>;
  stop(): Promise<void>;
  resumeContractIfPaused(): Promise<void>;

  // 主入口
  processBatch(callbacks?: DaemonStreamCallbacks): Promise<number>;    // 返本轮吸收 inbox 消息数
  processWithMessage(msg: Message, callbacks?: StreamCallbacks): Promise<void>;
  retryLastTurn(callbacks?: StreamCallbacks): Promise<void>;
  chat(options?: ChatOptions): Promise<void>;                          // REPL 入口（详 modules）

  // 中断 / 状态
  abort(): void;                     // 触发 UserInterrupt
  getStatus(): { turnCount: number; initialized: boolean; /* ... */ };
  getAsyncTaskSystem(): AsyncTaskSystem;       // phase 429 收 / concrete 直返
  getAuditWriter(): AuditLog;        // phase 429 收 / concrete 直返
}

// phase 429 已删 12 port interface（AuditPort / SnapshotPort / SessionStorePort / InboxPort /
// OutboxPort / ToolRegistryPort / ToolExecutorPort / ContextInjectorPort / ExecContextPort /
// ContractManagerPort / TaskLifecyclePort / SkillRegistryPort）/ RuntimeDependencies 直 dep concrete

export interface RuntimeDependencies {
  // L1
  readonly systemFs: FileSystem;
  readonly clawFs: FileSystem;
  // L2 deps
  readonly auditWriter: AuditLog;
  readonly snapshot: Snapshot;
  readonly sessionManager: DialogStore;        // 字段名 sessionManager / type DialogStore（字段名 rename align canonical 推 r+1 phase）
  // phase 467+ NEW (regime 切换协调装配 / Assembly own factory / per L5.G1-G4 closure 2026-05-07)
  readonly dialogStoreFactory: (systemPrompt: string) => DialogStore;   // regime 切换时 Runtime 调此 factory 装配 new instance（M#2 装配业务归 Assembly own / Runtime 不 own dir/filename/clawId 等装配 const）
  readonly inboxReader: InboxReader;
  readonly outboxWriter: OutboxWriter;
  // L2-L5 (capability provider)
  readonly llm: LLMOrchestrator;
  readonly toolRegistry: ToolRegistry;
  readonly toolExecutor: IToolExecutor;
  readonly skillRegistry: SkillSystem;         // 字段名 skillRegistry / type SkillSystem（字段名 rename align canonical 推 r+1 phase / 同 sessionManager + contractManager 同型）
  readonly contextInjector: ContextInjector;
  readonly execContext: ExecContext;
  // L4
  readonly contractManager: ContractSystem;    // 字段名 contractManager / type ContractSystem（字段名 rename align canonical 推 r+1 phase）
  readonly taskSystem: AsyncTaskSystem;
  // 可选注入（phase 182 setter 双阶段消除）
  readonly parentStreamLog?: StreamLog;
  readonly contractNotifyCallback?: (type: string, data: Record<string, unknown>) => void;
}

export interface RuntimeOptions {
  clawId: string;
  clawDir: string;
  llmConfig: LLMOrchestratorConfig;
  dependencies: RuntimeDependencies;
  maxSteps?: number;
  toolProfile?: ToolProfile;
  toolTimeoutMs?: number;
  subagentMaxSteps?: number;
  maxConcurrentTasks?: number;
  idleTimeoutMs?: number;          // 0 = 禁用
  // identity 注入
  systemPromptBuilder?: () => Promise<string>;
  identityToolFilter?: (registry: ToolRegistry) => void;
  // phase 467+ NEW (G1-G4 closed by 原则 derive 2026-05-07 / 待 code phase 落地)
  regimeSwitchStrategy?: 'all' | 'none' | 'last-turn';   // G1+G2 / default 'all' (D1c+D2+D6) / G3 触发：每 turn 末自动检测（_runReact 末用 turn 起 build 的 systemPrompt 比对 / 0 新 API） / G4 corner case：自动 repair 复用 DialogStore.repair（0 新 API）
}

export interface StreamCallbacks {
  onBeforeLLMCall?: () => void;
  onTextDelta?: (delta: string) => void;
  onTextEnd?: () => void;
  onThinkingDelta?: (delta: string) => void;
  onToolCall?: (toolName: string, toolUseId: string) => void;
  onToolResult?: (toolName: string, toolUseId: string, result: { success: boolean; content: string }, step: number, maxSteps: number) => void;
  onTurnStart?: (sources: Array<{ text: string; type: string }>) => void;
  onTurnEnd?: () => void;
  onTurnError?: (error: string) => void;
  onTurnInterrupted?: (cause: string, message?: string) => void;
  onProviderInfo?: (info: { name: string; model: string; isFallback: boolean }) => void;
  onProviderFailover?: (info: { from: string; timeoutMs: number }) => void;
  onProviderFailed?: (info: { provider: string; model: string; error: string }) => void;
}

export interface DaemonStreamCallbacks extends StreamCallbacks {
  onInboxMessages?: (messages: InboxMessage[]) => Promise<void>;
}

// 工厂
export type CreateRuntimeOptions = RuntimeOptions & { identity: 'motion' | 'claw' };
export function createRuntime(options: CreateRuntimeOptions): Runtime;
export function buildMotionSystemPrompt(opts: { contextInjector: ContextInjector; systemFs: FileSystem }): Promise<string>;

// Heartbeat（应然原 silent / 实然辅助 service）
export class Heartbeat {
  constructor(opts: HeartbeatOptions);
  start(): void;
  stop(): void;
}
export interface HeartbeatOptions {
  // 详 modules
}
export function createHeartbeat(opts: HeartbeatOptions): Heartbeat;

// Last-exit summary helpers (应然原 silent / 实然装配期使用)
export function summarizeLastExit(/* ... */): string;
export function readLastExitEvent(/* ... */): Promise<unknown>;
```

注：`ContractSystem` 名 align canonical（arch §22 + 表 1）。

**使用语义**：

- `initialize()` 必先调（once-only / 完成 dialog 复原 + AsyncTaskSystem.initialize 等子模块复原）
- `processBatch` / `processWithMessage` / `chat` 必在 initialize 后调
- `stop()` 释放资源 / 调后 instance 不可再用
- `abort()` 触发 UserInterrupt cause / 透 AgentExecutor onTurnInterrupted 钩子传给 caller
- `processBatch` 返本轮吸收 inbox 消息数（0 = 空轮）/ 每轮 turn 结束 snapshot commit（caller 不感知）
- 每次 LLM 调用后必透过 DialogStore 落盘 messages 数组（AgentExecutor stepCallback hook 内）/ 中断后从最后一次完整 LLM 调用恢复
- Runtime 不分 motion / claw 身份分支 / identity 经 systemPromptBuilder + identityToolFilter 注入
- DaemonStreamCallbacks 扩 onInboxMessages（消费方 await 内 callback / Runtime 不并发 batch）

**归本模块**：clawforum 常驻 agent 长期循环的唯一入口。业务模块要让 agent 「长期跑」必经本模块。

**不归本模块**：

- 模块装配（L1-L4 instances 构造），归 L6 Assembly
- agent 业务身份（motion / claw 分支），归 Assembly 注入 systemPromptBuilder + identityToolFilter（Runtime 内部不分支）
- 单步执行算法（一次 LLM 调用加 tool 派发），归 L3 StepExecutor
- agent 完整循环算法（多步调度加停止判定），归 L3 AgentExecutor（Runtime 是 caller / 写 stepCallback）
- 异步任务调度，归 L4 AsyncTaskSystem
- 契约状态机（生命周期管理加验收加重试），归 L4 ContractSystem
- 单步 LLM 调用容错，归 L2 LLMOrchestrator
- 工具实现加权限校验，归 L2 Tools
- dialog 持久化磁盘 I/O，归 L2 DialogStore（Runtime 是 caller）
- snapshot git 提交，归 L2 Snapshot

**不可消除耦合理由**：M#1 单一职责（事件驱动循环跟装配跟单步执行跟循环算法跟任务调度跟契约状态机都是独立可变职责）加 M#2 业务语义归属（事件循环加 turn 边界加中断响应加轮级 commit 加 dialog 落盘协调语义归本模块）derive 自 Design Principle「运行中断即从最后一次完整 LLM 调用恢复状态并继续」加 D11「motion 为 clawforum 中一个特殊的 claw」（Runtime 内部无身份分支 / Assembly 装配期注入 identity 差异 / KD#24）— Runtime 是 long-running service 的唯一发起点，把 agent 循环包装成可生命周期管理的常驻服务。

> **应然立场注（phase 457 + 2026-05-07 G1-G4 closed by 原则 derive + phase 539 sharpen identity-only diff）**：Runtime own「**system prompt 变更检测加 regime 切换协调**」业务（per modules/l5_runtime.md §1+§2 + §A.r57-1）。检测 **identity 段**（agents + skills）变化 → `dialogStore.archive()` 当前 + 装配 `new DialogStore(..., newSystemPrompt)` + messages 继承（**default 'all' 全继承** / `regimeSwitchStrategy?: 'all' | 'none' | 'last-turn'` 枚举 caller override）。derive 自 l2_dialog_store §A.r53-1 应然立场（DialogStore 1 instance = 1 system prompt regime / caller own regime 切换业务）。**identity 层 vs 动态层立场**（phase 539 sharpen）：identity = agents + skills（智能体是谁 + 能做什么 / 改即 regime change）/ dynamic = memory + contract（同 regime 内自然演进 / 不触发）/ 派生 Philosophy「Agent 即目录」+ M#7 + D6。**已 align（phase 521 落地 `65e8fdd3` + phase 539 修正 `81765e72`）**：ContextInjector 加 `buildSystemPromptForRegime(): {full, identityHash}` / Runtime `_runReact` + `chat()` 末 1 处 check 用 identityHash 比对 lastIdentityHash → 变化触发 `_performRegimeSwitch` → `dialogStore.archive()` + `dialogStoreFactory(newSystemPrompt)` 装配 new instance + per strategy 继承 messages（'all' / 'none' / 'last-turn'）+ `DialogStore.repair` 修 tool_use 悬空 + audit `regime_switch` event / `_checkRegimeSwitch` 整段 try/catch + audit `regime_switch_failed` + lastIdentityHash 不更新自愈（D7+D2 软降级）/ 4 turn entry（processBatch / processWithMessage / retryLastTurn / chat）自动 cover / 自定义 systemPromptBuilder fallback 走整段比对（兼容 phase 521 / U3 (a) 0 caller YAGNI）/ design+code 联动 **5 阶段闭环**（phase 457 + L2 前置 466 + L5.G1-G4 closure 2026-05-07 + code 521 + 缺陷修 539）。

**Design-gap（跨模块 / 应然 silent / 待用户补 / phase 457 浮出 / 0 mechanical 决定）**：

Runtime regime 切换协调业务应然 sharpen 完成 / 但具体 messages 继承 strategy + 接口形态 + 触发时机 + corner case 处置应然 silent。4 design-gap 由原则 derive closed（2026-05-07 / 用户拍板 (a)(a)(a)(a) 全 (a)）：

| Gap | 业务概念 | 决策 (a) | 原则核 |
|---|---|---|---|
| **L5.G1** messages 继承默认 strategy | regime 切换时 new instance 起步 messages 来源 | ✅ **(a) 全继承 default** | D1c 中断可恢复 + D2 未经显式决策不丢弃 + D6 智能体决策主体（messages = agent 记忆不应外部截断）+ Philosophy「上下文工程」 |
| **L5.G2** strategy 接口形态 | caller 怎么 override default | ✅ **(a) 枚举字符串 `'all' \| 'none' \| 'last-turn'` / default 'all'** | M#9 编译器优先 + M#8 最小表面 + YAGNI（实然 0 caller 需 override / 反 over-engineering / 同 phase 458 STALE 推翻判据）/ 反对 (b) callback 1 impl + 0 ROI / 反对 (c) 混合双表面 / 未来真出现 caller 需 callback 时升档 callback 形态（per M#11）|
| **L5.G3** 触发时机 | Runtime 何时检测 system prompt 变化 | ✅ **(a) 每 turn 末自动检测**（用 _runReact 起 build 的 systemPrompt 比对 lastSystemPrompt / 不重调 buildSystemPrompt）| D8 事件驱动「系统在智能体需要决策时交付」+ M#1 业务归 Runtime own（caller 不知 system 变了否）+ M#8 0 新表面 / **turn 末检测优势**：(1) regime change turn 内 0 LLM latency 阻塞（archive + new instance + repair 装配在 turn 边界）/ (2) agent 体验平滑（turn N 用 v1 messages + v_N system / turn N+1 起新 instance）/ (3) 冷启动 0 额外（首 turn 直接进 _runReact / 末尾 set lastSystemPrompt）/ (4) 实施简单（_runReact 末 1 处 check / 4 turn entry 自动 cover）/ 反对 (b) trigger API 业务归属错位 / 反对 (c) 混合 over-engineering |
| **L5.G4** tool_use 引用已删工具 corner case | 全继承时 / new system 删了 old messages 含 tool_use 的 tool | ✅ **(a) 自动 repair 复用 DialogStore.repair** | D1c 中断可恢复 + D2 不丢弃（修复非丢弃）+ D6 agent 不感知干扰 + M#7 模板复用 + D5 显式 design 决策修复 / 反对 (b) 抛错（反 D1c+D2）/ 反对 (c) silent ignore（反 D5）|

**真合规判定**：✅ **4 gap 全 closed by 原则 derive（2026-05-07）** / G2 决策反 over-engineering（同 phase 458 模板）/ phase 467+ code phase 起草触发条件齐全（DialogStore L2 前置 ✅ phase 466 完成 / SHA `201bc6df` / ctor +systemPrompt + restorePrefix method）/ design only / 0 代码本次 closure。

---

## Cron [capability, DI]

**生产方**：`l5_cron`

**消费方**：

- `l6_assembly`（DI，装配期注入 CronJob[] + 工厂构造 CronRunner）
- `l6_daemon`（间接 / Daemon spawn `node cron-entry.js` 独立进程 / SIGTERM 终止）

**接口签名**：

```ts
export class CronRunner {
  constructor(jobs: CronJob[], audit: AuditLog);
  start(tickIntervalMs?: number): void;   // default 1000ms
  stop(): void;
  tick(): void;                            // 单 tick 触发 / 测试 + 派生用 / 应然原 silent
}

export type CronSchedule =
  | { type: 'daily'; time: string }       // "HH:MM" 每天固定时刻
  | { type: 'hourly' }                    // 每小时整点
  | { type: 'interval'; minutes: number }; // 每 N 分钟

export interface CronJob {
  name: string;
  enabled: boolean;
  schedule: CronSchedule;
  handler: () => Promise<void>;
}

export function parseSchedule(s: string, audit?: AuditLog): CronSchedule;
// 格式：'hourly' | 'daily:HH:MM' | 'interval:Nm'
// 未知格式 → fallback hourly + audit `cron_parse_fallback` (audit 可选 / 应然原 silent on audit 参 / 实然加为 fallback observability)

// 工厂
export function createCronRunner(jobs: CronJob[], audit: AuditLog): CronRunner;
```

注：`AuditLog` 来自 [L2 AuditLog](./l2a.md#auditlog-capability-di)。**应然 silent on `tick()` 公共 method + `parseSchedule` 第二 audit 参**（实然 underspec / 应然 align）。

**使用语义**：

- `start()` 默认 1000ms tick / 可经入参覆盖
- `stop()` 调后 instance 不可再用
- CronJob[] 装配期注入 / 运行期不变（runner 不知具体业务 / 装配方注入 schedule + handler）
- runKey 去重靠 lastRunKey Map / 重启重置（runner 派生态不落盘）
- 同 job 防重叠靠 running Set / handler 进出 add/delete
- jobs 必须设计为幂等（重启场景下重跑无副作用）
- schedule 仅 3 形态 / 不解析复杂 cron 表达式
- 不预设跨 daemon 协调（per-runner instance）
- 失败语义：
  - parseSchedule 未知格式 → fallback hourly + audit `cron_parse_fallback`（不抛）
  - job handler 异常 → catch + audit `cron_job_error` / runner 不终止 / 继续下一 job
  - LLM 调用 / disk 阈值 / 业务通知等失败语义归各业务 job 自管（runner 不感知）

**归本模块**：clawforum 定时任务调度框架的唯一入口。业务模块要按时间表触发任务必经本模块。

**不归本模块**：

- 业务任务语义（具体 cron job 业务逻辑：dream-trigger / disk-monitor / llm-stats），归各业务模块自己 own / Cron 只 own 触发机制
  derive 自 M#5「底层模块不预设上层模块语义」
- 跨进程协调（per-daemon instance 不预设跨 daemon 协调）
- 持久化任务历史（Cron runtime ephemeral / 去重时窗状态重启 reset / D4 显式豁免）
- 任务结果回传（Cron 仅触发处理器 / 处理器内部业务归处理器 caller）
- 复杂 cron 表达式解析（仅 3 形态）
- LLM 调用主路径（仅 dream jobs 内部消费 / runner 不知）
- 异步任务派发（具体 cron job 内部若需派子代理走 L4 AsyncTaskSystem）
- 跨进程通信（disk-monitor 投 motion inbox 走 L2 Messaging InboxWriter）

**不可消除耦合理由**：M#1 单一职责（定时调度框架跟具体业务任务跟跨进程协调跟任务持久化都是独立可变职责）加 M#2 业务语义归属（tick 调度加 runKey 去重加 job 防重叠加 job 异常隔离语义归本模块）derive 自「让定时调度可靠」业务 invariant 加 M#5「底层模块不预设上层模块语义」— Cron 是 generic 调度 primitive，jobs 业务由装配方注入，让多种业务（记忆整合 / 监控告警 / 统计聚合）共享同一调度框架。

---

## StatusService [capability, direct]

**生产方**：`l5_status_service`（src/core/status-service/）

**消费方**：

- `l6_assembly`（direct，装配期 register status 工具到 ToolRegistry + 注入 deps）
- `l6_cli`（direct / 综合 status 命令调 collect / 避免重新实现聚合视图 / arch §28 表 1 caller 已加）

> 上游 deps（StatusService 调它们）：L1 FileSystem（clawspace 概览 read）+ L2 ToolProtocol（status 工具 schema）+ L4 ContractSystem（loadActive read / phase 458 后 statusTool 直 dep / 0 port abstraction）+ L4 AsyncTaskSystem（listRunning + listPending read）— 详见 arch §28 表 1 «依赖» 列。

**接口签名**（物理位置 src/core/status-service/ / phase 458 简化后 = statusTool 单一 export 形态）：

```ts
// agent 工具 + 模块自治命名空间（业务工具归 StatusService own / phase 446 物理立 / phase 458 删 ContractStatusPort 抽象 + inline 所有聚合逻辑进 execute）
export const statusTool: Tool & { contractSystem?: ContractSystem };  // phase 458: 直 dep ContractSystem class（顺向 L5 → L4 / 合 M#5）/ contractSystem 是 attached 字段 Assembly 装配期注入（不经 ctor）
export const STATUS_TOOL_NAME: string;

export const STATUS_AUDIT_EVENTS = {
  CONTRACT_ERROR: 'status_contract_error',
  TASK_PENDING_ERROR: 'status_task_pending_error',
  TASK_RUNNING_ERROR: 'status_task_running_error',
} as const;

// phase 458: 删 ContractStatusPort + ContractStatusView + ContractStatusItem + ContractStatusItemStatus 4 type
// （`feedback_governance_workaround_smell §5` cluster 第 8 例 STALE 推翻 / 1 impl + 0 ROI + L4 → L5 反向 import 全消除）
// statusTool.execute 内部 inline 聚合：直调 ContractSystem.loadActive() + 内联 doneCount/totalCount/items 计算（移自删除的 status-port-impl.ts）+ 直读 TASKS_PENDING_DIR/TASKS_RUNNING_DIR + clawspace ls + MEMORY.md size 单源 try/catch 软降级
```

**应然 silent on `StatusService` interface + `collect()` method + `StatusSnapshot` + `ActiveContractSummary` + `ClawspaceOverview` + `SourceWarning` + `StatusServiceDeps` + `createStatusService` factory 8 应然幻象**（phase 446 物理立时雏形 plan / phase 458 简化 inline 后实然只剩 statusTool 单 export / 应然 rule 必有现实功能依据反向 / 删幻象 align 实然）/ CLI 综合 status 命令直调 statusTool.execute 复用聚合逻辑（不需要独立 collect() abstract layer / phase 458 ContractStatusPort 推翻同型）。

注：`Tool` 来自 [L2 ToolProtocol](./l2b.md#toolprotocol-type-only) / `ContractSystem` + `AsyncTaskSystem` 来自 [L4](./l4.md)。

**使用语义**：

- `statusTool.execute()` 内部直接聚合三源（ContractSystem.loadActive + tasks/pending+running ls + clawspace 概览）/ 0 入参 / 0 副作用 / read-only
- read-only 不变量：本模块不修改任何业务 state / 仅 read 各业务模块 typed API
- 多源聚合策略：每源独立 try/catch / 失败 audit `STATUS_*_ERROR` + 软降级文本（如「Contract: Error loading」）/ 不阻塞其他源
- contractSystem 经 statusTool 字面 attach 字段注入（Assembly 装配期 `statusTool.contractSystem = ...`）/ 不经独立 ctor 或 deps interface
- profile 准入：`full`（motion + claw 主代理）含 status / `subagent`/`miner`/`verifier`/`dream` 不含

**归本模块**：clawforum agent 自我状态聚合 introspection 的唯一入口。agent 查询自我状态加 status 工具 schema + execute 实现必经本模块。

**不归本模块**：

- 业务 state owner（contract / task / dialog state 归各业务模块）
- state 修改（read-only / 不写）
- daemon 进程 state（PID / lockfile 归 L6 Daemon + L2 ProcessManager）
- watchdog state（独立进程 / 归 L6 Watchdog）
- CLI 综合 status 命令实现（CLI L6 own / 但 dep 本模块 collect）
- agent 决策替代（仅提供决策所需信息）

**不可消除耦合理由**：M#1 单一职责（read-only state 聚合 introspection 跟各业务模块 state owner 是独立可变职责）加 M#2 业务语义归属（status 工具 schema + execute + 多源聚合视图语义归本模块）derive 自 Philosophy「系统为智能体服务」加 Design Principle「智能体是决策主体」+「用户可观察」— status 工具是 agent 自我观察 + 用户综合查看的统一聚合点 / 两 caller（agent + CLI）共用 collect 逻辑避免双源 drift。

---

## Gateway [capability, DI]

> 数据面交互门面 / 与 CLI（控制面）并列。Gateway interface + class + factory / 5 method 实施（start / stop / askUser / getActiveConnections / isOnline）/ 详 [modules/l5_gateway.md](../modules/l5_gateway.md)。

**生产方**：`l5_gateway`

**消费方**：

- `l6_assembly`（DI / 按需装配 / 实然 motion-only — 见 [modules/l5_gateway.md](../modules/l5_gateway.md) §7.B B.4）
- `l6_daemon`（间接 / Daemon 通过 ctor 注入 `interrupt: (reason) => void` callback function / 函数类型注入 / 0 module dep）

> 上游 deps（Gateway 调它们）：L1 Transport（可选 / 无则进入 offline mode）+ L2 Stream（streamFactory 注入）+ L2 AuditLog + L2 Tools（ask_user 工具注册）— 详见 arch 表 1 «依赖» 列。

**接口签名**：

```ts
export interface Gateway {
  start(): Promise<void>;                              // 绑 transport 回调 + 启 StreamReader / 重复调用抛错 / offline 下 no-op
  stop(): Promise<void>;                               // 先停 reader / drop 连接 / close transport / idempotent
  askUser(question: string, ctx: ExecContext): Promise<ToolResult>;  // Step 2 占位 (实然抛 Error 'ask_user not implemented')
  getActiveConnections(): readonly Connection[];       // 当前连接快照 / 不暴露引用
  isOnline(): boolean;                                  // online/offline 一次性定型
}

export interface GatewayInput {
  /** StreamReader 工厂；Gateway 注入 onEvent 回调后调用 start */
  streamFactory: (onEvent: (event: StreamEvent) => void) => StreamReader;
  /** 已处于 listening 状态的 Transport / undefined = offline */
  transport?: Transport;
  /** Daemon 注入的 interrupt 回调 */
  interrupt: (reason: 'user') => void;
  /** askUser 超时（Step 3 使用） */
  askUserTimeoutMs?: number;
  /** audit 写入器 / Gateway 用于写 10 类结构化事件 */
  audit: AuditLog;
}

// 工厂
export function createGateway(input: GatewayInput): Gateway;
```

详细 ClientMessage / ServerMessage discriminated union + ask_user 工具实装细节归 [modules/l5_gateway.md](../modules/l5_gateway.md) §1 + §3 + §5（askUser 当前为占位实施 / 完整实装待后续 phase）。

**使用语义**：

- online / offline 一次性定型（`isOnlineMode = (transport !== undefined)` 启动期定 / 运行期不变）
- offline 下 ask_user 立即返 failureResult
- **数据面 vs 控制面分离**：CLI 唯一对外入口语境限定**控制面**（daemon 生命周期）/ Gateway 承担**数据面**（运行中 agent ↔ 外部客户端实时交互）/ 两者并列不冲突
- interrupt debounce：窗口内重复 trigger drop（不 emit 不 drop 连接）
- 连接生命周期：onConnect 加入 connections Map / onDisconnect 移除 / dropConnection 主动清理（broadcast 失败 / malformed JSON / unknown type）
- 失败语义：
  - broadcast 失败 → audit `gateway_connection_dropped` + dropConnection
  - malformed JSON / unknown msg type → dropConnection + audit
  - ask_user timeout / abort → audit `gateway_ask_user_cancelled` + 返 failureResult

**归本模块**：clawforum 数据面交互门面（外部客户端 ↔ 内部系统实时交互）的唯一入口。

**不归本模块**：

- 消息业务语义（Transport 只传 opaque string / Gateway 仅 JSON parse + discriminated union dispatch），归各 caller
- 连接鉴权 / 限流，归 Transport 或未来安全层
- StreamEvent 格式，归 L2 Stream
- StreamReader 构造（通过 streamFactory 注入）
- backpressure buffer（Transport 层 best-effort / Gateway 不做高层重传）

**不可消除耦合理由**：M#1 单一职责（数据面交互门面跟 Transport 实现跟 Stream 实现跟控制面 CLI 都是独立可变职责）加 M#2 业务语义归属（askUser 加 stream 订阅 加 interrupt 路由 加 连接生命周期语义归本模块）derive 自 Design Principle「用户可以观察运行过程中的所有状态」加 D8「事件驱动」加 D11「motion 单向访问」— Gateway 是数据面唯一发起点，让外部客户端与系统实时双向交互，与 CLI 控制面并列不冲突。

---

## Callback signatures

- Daemon interrupt callback（L5 Gateway own，L6 Daemon 实现）— 待写
