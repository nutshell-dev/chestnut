
/** Maximum output lines cap for viewport */
export const OUTPUT_LINES_CAP = 5000;
/**
 * chat-viewport 周期 rescan claws 目录间隔（ms）.
 * 检测新创建的 claw / 新 contract 信号源.
 */
export const CLAW_SCAN_INTERVAL_MS = 2000;
