// Thin wrapper — re-exports from canonical owner foundation/utils/format.ts
// During migration. Once all consumers import from canonical source, this file is deleted.

export { SUMMARY_MAX_CHARS } from '../foundation/utils/format.js';
