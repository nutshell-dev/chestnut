import type { AssembleConfig, Instances } from './index.js';

export async function assemble(_config: AssembleConfig): Promise<Instances> {
  throw new Error('Assembly.assemble: not implemented (Phase 154 Step 2)');
}
