/**
 * phase 63: composer for inbox type `contract_crashed` — NO_GUIDANCE sentinel.
 * Step E 将替换为 real composer.
 */

import { NO_GUIDANCE } from '../types.js';

export const composer = NO_GUIDANCE;
